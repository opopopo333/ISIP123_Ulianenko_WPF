<Page x:Class="WpfApp1.Pages.BuilderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="BuilderPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#2D2D30" Grid.Row="0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">

                <Button x:Name="BtnGoToHistory" Content="Сохраненные сборки" Margin="5" Padding="15,5" Background="White" Click="BtnGoToHistory_Click_1"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBox x:Name="SearchBox" Width="200" Margin="0,0,10,0" VerticalAlignment="Center" Tag="Поиск по названию..." TextChanged="SearchBox_TextChanged"/>

                    <TextBlock Text="Производитель:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox x:Name="ManufacturerFilter" Width="120" Margin="0,0,10,0" VerticalAlignment="Center" SelectionChanged="Filter_SelectionChanged"/>

                    <TextBlock Text="Тип детали:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox x:Name="TypeFilter" Width="120" VerticalAlignment="Center" SelectionChanged="Filter_SelectionChanged"/>
                </StackPanel>

                <ListView x:Name="CatalogList" Grid.Row="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#DCDCDC" BorderThickness="1" Margin="4,2" Padding="10" CornerRadius="5" Background="White" Width="600">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Image Grid.Column="0" 
                   Source="{Binding image}" 
                   Height="80" Width="80" 
                   Stretch="Uniform" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Center"
                   Margin="0,0,10,0"/>

                                    <StackPanel Grid.Column="1">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding name}" FontWeight="Bold" FontSize="14" Margin="5,0,0,0"/>
                                        </StackPanel>

                                        <TextBlock Text="{Binding parttype_.name}" Foreground="Gray" FontSize="11" Margin="0,2,0,5"/>

                                        <WrapPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding DisplayInfo}" 
           FontSize="12" 
           Foreground="#666" 
           TextWrapping="Wrap"/>
                                        </WrapPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="20,0,0,0">
                                        <TextBlock Text="{Binding price, StringFormat={}{0:F0} ₽}" 
                           FontWeight="Bold" FontSize="16" Foreground="Green" 
                           TextAlignment="Right" Margin="0,0,0,5"/>
                                        <Button Content="Добавить" Width="90" Height="28" 
                        Click="BtnAddPart_Click" Background="#007ACC" 
                        Foreground="White" Cursor="Hand">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="3"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <Border Grid.Column="1" Background="#F4F4F5" Margin="0,10,10,10" CornerRadius="5" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Текущая сборка" FontSize="18" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,10" HorizontalAlignment="Center"/>

                    <ListView x:Name="AssemblyList" Grid.Row="1" Background="Transparent" BorderThickness="0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="5,8" Width="200">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="35"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Button Grid.Column="0" Content="✕" Click="BtnRemovePart_Click" 
                    Width="24" Height="24" Background="Transparent" 
                    Foreground="LightCoral" FontWeight="Bold" BorderThickness="0" 
                    VerticalAlignment="Center">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="12"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="5,0">
                                            <TextBlock Text="{Binding name}" FontWeight="SemiBold" FontSize="13" Foreground="#333" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding parttype_.name}" FontSize="10" Foreground="Gray"/>
                                        </StackPanel>

                                        <TextBlock Grid.Column="2" Text="{Binding price, StringFormat={}{0:F0} ₽}" 
                       VerticalAlignment="Center" Foreground="#666" FontSize="12"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <StackPanel Grid.Row="2" Margin="0,10,0,0">
                        <TextBlock x:Name="CompatibilityWarningText" Text="⚠️ Ошибка совместимости!" Foreground="Red" FontWeight="Bold" Visibility="Collapsed" Margin="0,0,0,10" TextWrapping="Wrap"/>

                        <TextBlock x:Name="TotalPriceText" Text="Итого: 0 ₽" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,0,0,10"/>

                        <TextBox x:Name="AssemblyNameBox" Text="Моя сборка" Margin="0,0,0,5" Padding="2" ToolTip="Название сборки"/>
                        <TextBox x:Name="AuthorNameBox" Text="Имя автора" Margin="0,0,0,10" Padding="2" ToolTip="Ваше имя"/>

                        <Button x:Name="BtnSaveAssembly" Content="Сохранить сборку" Padding="10" Background="#007ACC" Foreground="White" FontWeight="Bold" Click="BtnSaveAssembly_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
