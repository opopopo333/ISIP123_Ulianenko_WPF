<Page x:Class="WpfApp1.Pages.HistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HistoryPage">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#2D2D30" Grid.Row="0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                <Button Content="Назад к сборке" Click="BtnBack_Click" Margin="5" Padding="15,5"/>
                <TextBlock Text="Архив сохраненных сборок" Foreground="White" VerticalAlignment="Center" Margin="20,0" FontSize="16"/>
            </StackPanel>
        </Border>

        <ListView x:Name="LViewAssemblies" Grid.Row="1" Margin="10" Width="900">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="LightGray" BorderThickness="1" Margin="0,5" Padding="10" CornerRadius="10" Width="869">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding name}" FontSize="18" FontWeight="Bold" Foreground="#007ACC"/>
                                <TextBlock Text="{Binding author, StringFormat='Автор: {0}'}" Foreground="Gray" Margin="0,2,0,5" />

                                <ItemsControl ItemsSource="{Binding partassembly_}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <BulletDecorator Margin="10,0,0,0">
                                                <TextBlock Text="{Binding basepart_.name}" FontSize="12"/>
                                            </BulletDecorator>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding TotalSum, StringFormat={}{0:N0} ₽}" 
                                   FontSize="16" FontWeight="Bold" TextAlignment="Right"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
